  # cat=basic/enable; type=boolean; label=List-Module: Enable exposure of tracked images as TCA-rows.
list_module_tca = 0
  # cat=basic/enable; type=boolean; label=URL-rewriting: Enable stripping of "typo3temp/imap/map/", and delegate internal URL-rewriting to mod_rewrite.
mod_rewrite = 0
  # cat=basic/enable; type=boolean; label=Images: Enable progressive saving for PNGs (ImageMagick may not support it!)
progressive_pngs = 0
  # cat=basic/enable; type=boolean; label=Images: Enable progressive saving for JPGs (ImageMagick may not support it!)
progressive_jpgs = 0
  # cat=basic/enable; type=boolean; label=Images: Enable progressive saving for GIFs (ImageMagick may not support it!)
progressive_gifs = 0
  # cat=basic/enable; type=boolean; label=Images: Enable stripping of image meta-data
strip = 0
